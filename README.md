# wp-roles-permissions
Allow a administrator to create new roles, delete, edit and assign roles to users. Allow an administrator to assign roles to posts and pages. Pages by default are viewable by everyone, the moment a role is assigned to the post or page, it become inaccessible to anyone without that role. 
